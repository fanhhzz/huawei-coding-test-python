while True:
    try:
        a,b=map(int, input().split())
        l=list(map(int, input().split()))
        l.sort()
        s=l[:b]
        re=[]
        for i in s:
            re.append(str(i))
        print(" ".join(re))
    except:break
